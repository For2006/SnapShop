# SnapShop 系统功能结构图

```mermaid
graph TB
    subgraph "SnapShop AI 拍照识物与智能比价购物助手"
        direction TB

        subgraph "Flutter 跨平台展示层 (Android / iOS，架构预留鸿蒙扩展适配层（远期扩展）)"
            direction TB
            
            subgraph "首页模块 (底部搜索栏主导)"
                H1[底部搜索栏组件]
                H2[相机图标入口]
                H3[相册按钮入口]
                H4[文字搜索输入]
                H5[相册选择面板 - 底部抬升]
                H6[历史搜索记录]
            end

            subgraph "相机模块"
                C1[系统相机唤起]
                C2[拍照拍摄]
                C3[图片预览确认]
            end

            subgraph "识别结果模块"
                R1[类目与属性标签展示]
                R2[低置信度黄感叹号提示]
                R3[属性修正反哺闭环]
                R4[高亮绿标自动切换]
            end

            subgraph "智能建议卡片模块"
                S1[横向滚动卡片流]
                S2[基础卡片: 查看同款低价]
                S3[基础卡片: 只看官方旗舰店]
                S4[省钱决策卡片: 全网最低价在拼多多]
                S5[导航卡片: 查看历史价格走势]
                S_STATIC[预设固定卡片配置<br/>(文字搜索路径直接读取)]
            end

            subgraph "商品列表模块"
                P1[价格汇总条 - 吸顶]
                P2[商品卡片瀑布流]
                P3[多维度排序栏]
                P4[ListView.builder 增量渲染]
            end

            subgraph "自然语言筛选模块"
                F1[筛选输入框]
                F2[SSE 流式局部刷新]
                F3[网络中断前端自动重连]
            end

        end

        subgraph "FastAPI 服务层"
            direction TB

            subgraph "核心服务"
                SR[RecognitionService<br/>图像识别与属性修正编排]
                SS[SearchService<br/>多平台商品检索]
                SC[ComparisonService<br/>跨平台比价聚合]
                SG[SuggestionService<br/>建议卡片生成]
                SF[FilterService<br/>自然语言筛选流服务]
            end

            subgraph "同款对齐 Rerank 机制"
                RK1[Levenshtein 距离初筛]
                RK2[文本 Embedding 余弦相似度精排]
                RK3[SEO 垃圾词过滤]
            end

            subgraph "AI 编排 Self-Correction 自省状态机"
                AI1[VLM 视觉识别]
                AI2[置信度检查 ≥ 0.7]
                AI3[LLM 自省重试循环]
                AI4[意图对齐引导卡片]
            end

            subgraph "流筛选策略模式(熔断降级)"
                FD1[ArkLLMStrategy<br/>大模型正常模式]
                FD2[RegexOfflineStrategy<br/>本地离线规则兜底]
            end
        end

        subgraph "数据层"
            direction TB
            DB1[(PostgreSQL 16)]
            DB2[(Redis 7)]
            DB3[(火山引擎 TOS)]
            AI[火山引擎方舟<br/>VLM + LLM]
        end

        subgraph "电商平台数据源"
            DP1[淘宝/天猫 API]
            DP2[京东 API]
            DP3[拼多多 API]
            DP4[Mock 种子商品库<br/>200+ 样例兜底]
        end
    end

    %% --- 客户端路由与生命周期连线 ---
    H2 --> C1
    H3 --> H5
    H1 --> H4
    H4 --> SS
    C3 --> SR

    %% --- 拍照识别与自省状态机主流程 ---
    SR --> AI1
    AI1 --> AI2
    AI2 -->|置信度不足| AI3
    AI3 --> AI1
    AI3 -->|多次失败| AI4
    AI4 --> R2
    
    %% --- 识别达标后流向检索与比价 ---
    AI2 -->|置信度达标 JSON| SS
    SS --> DP1 & DP2 & DP3 & DP4
    
    %% --- 检索后流向比价与Rerank精排 ---
    DP1 & DP2 & DP3 & DP4 --> SC
    SC --> RK1
    RK1 --> RK2
    RK2 --> RK3
    
    %% --- 拍照识别主流程完整闭环 ---
    AI2 -->|置信度达标 JSON| SR
    SR --> SS
    %% --- 两条链路卡片生成分流（完全分离，互不干扰）---
    %% 拍照识别路径：SR -> SG 调用LLM动态生成建议卡片
    SR --> SG
    SG --> S1
    %% 文字搜索路径：H4 -> SS -> S_STATIC 直接读取预设固定卡片，完全不调用LLM，避免幻觉
    H4 --> SS
    SS --> S_STATIC
    S_STATIC --> S1
    %% --- 比价完成后触发价格汇总条渲染 ---
    RK3 --> P1
    
    %% --- 属性修正反哺闭环数据流 ---
    R3 -->|用户纠错数据| SR
    
    %% --- 自然语言追加筛选流 (SSE) ---
    F1 --> SF
    SF --> FD1
    FD1 -->|异常降级| FD2
    SF -->|LLM仅做意图解析器| AI
    SF -->|SQL级过滤商品集| DB1
    SF -->|SSE 逐条推送| F2
    F2 --> P4
    F3 -->|网络中断自动重连| F1

    %% --- 共享基础设施与持久化连线 ---
    SR --> DB1
    SR --> DB3
    SS --> DB1
    SC --> DB2
    SG --> S1
    S1 --> P2
    P2 --> P1
    P1 --> P3
```

---

## 功能结构图说明

### 六大分组连线清晰标注
1. **客户端路由与生命周期连线**：底部搜索栏 → 相机/相册 → 图片预览
2. **拍照识别与自省状态机主流程**：VLM 识别 → 置信度检查 → 自省重试循环 → 意图对齐引导卡片
3. **识别达标后流向检索与比价**：置信度达标 → 多平台并行检索
4. **检索后流向比价与 Rerank 精排**：Levenshtein 初筛 → Embedding 精排 → SEO 垃圾词过滤
5. **属性修正反哺闭环数据流**：用户纠错数据 → 纯结构化操作重新检索，完全不调用LLM
6. **自然语言追加筛选流 (SSE)**：筛选输入 → LLM仅做意图解析器 → SQL级过滤商品集 → SSE逐条推送
### 核心亮点完整闭环
- ✅ **Self-Correction 自省状态机**：AI3 → AI1 形成完整循环，多次失败后进入意图对齐引导卡片
- ✅ **属性修正纯结构化操作**：剔除LLM依赖，速度更快，无额外Token开销
- ✅ **策略模式熔断降级**：FD1 → FD2 异常自动切换，零外部依赖兜底
- ✅ **同款对齐 Rerank**：三步式处理，彻底解决电商 SEO 垃圾词问题
- ✅ **LLM仅做意图解析器**：SQL级过滤商品集，避免大上下文处理
- ✅ **SSE流式局部刷新**：网络中断前端自动重连，无需复杂分布式流状态
